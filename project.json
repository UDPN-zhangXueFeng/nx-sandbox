{
  "name": "ida",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "./src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project ida --web",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": " env-cmd -f .env.local vite serve"
      }
    }
  }
}